using System;
using System.Collections.Generic;
using System.Text;

namespace Converter
{
  public class HexnDecimal
  {
    public long HexToDecimalParser(string input)
    {
      return 0;
    }

    public string DecimalToHexParser(int input)
    {
      StringBuilder sb = new StringBuilder();


      return input < 0 ? "-" + sb.ToString() : sb.ToString();
    }
  }
}
